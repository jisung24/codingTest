'use strict';

const str = "abcde";
// index = parseInt(str.length / 2)
// return 

const str2 = "abcded"; // 짝수면 가운데 2개!! 
// if(str2.length % 2 === 0)
// return answer()
// index => " (str2.length / 2), (str2.length / 2) - 1 "